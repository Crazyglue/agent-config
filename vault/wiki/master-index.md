# Master Index

## Topics

- [[example/_overview|An Example]] — This is an example description

## Conventions

- Wiki entries are reference docs, not transcripts. Distill from session memos.
- Every detail page must be linked from its topic's `_overview.md` — orphans don't exist.
- Update this file whenever a topic is added.
- All dependency versions in publishable workspace packages should use **exact versions** (no `^`/`~`) to ensure deterministic installs outside workspace context.
- See vault `AGENTS.md` for full conventions, auto-recall, eager-distillation, and lint workflows.
